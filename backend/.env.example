# Database (Docker postgres — host port 5433)
DATABASE_URL="postgresql://quyoshli:CHANGE_ME@localhost:5433/quyoshli?schema=public"

# App
PORT=3000
NODE_ENV=development

# JWT
JWT_ACCESS_SECRET="CHANGE_ME"
JWT_REFRESH_SECRET="CHANGE_ME"
JWT_ACCESS_TTL="15m"
JWT_REFRESH_TTL="30d"

# OTP
OTP_TTL_SECONDS=120
OTP_DEV_MODE=true   # true => kodni log qiladi, SMS yubormaydi

# SMS (Eskiz.uz — placeholder, dev rejimda ishlatilmaydi)
ESKIZ_EMAIL=""
ESKIZ_PASSWORD=""
ESKIZ_FROM="4546"

# Static files
UPLOADS_DIR="uploads"
PUBLIC_BASE_URL="http://localhost:3000"

# Telegram bot (buyurtma bildirishnomalari)
TELEGRAM_BOT_TOKEN=""
TELEGRAM_ORDER_CHAT_ID=""
ADMIN_IDS=""
